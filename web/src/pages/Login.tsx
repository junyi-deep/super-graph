import { FormEvent,useState } from "react";import { api } from "../api";import type { User } from "../types";
export function Login({onLogin}:{onLogin:(u:User)=>void}){const [username,setUsername]=useState("");const [error,setError]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();try{onLogin(await api.login(username))}catch(e){setError(e instanceof Error?e.message:"登录失败")}};return <main className="login"><form onSubmit={submit}><h1>Super Graph</h1><p className="muted">团队画板与 Mermaid 图表协作空间</p><label>用户名<input autoFocus value={username} onChange={e=>setUsername(e.target.value)} maxLength={64}/></label><button>登录</button>{error&&<p className="error">{error}</p>}<small>可信内网身份标签，不是强身份认证。</small></form></main>}
